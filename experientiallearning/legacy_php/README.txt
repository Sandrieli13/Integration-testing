Original PHP entry points from the standalone site (login, MySQL-backed display, charts with config.php).
They are not executed by Django. Use /experiential/ URLs and Django views instead.
Restore these files next to a PHP runtime and MySQL if you need the legacy behavior.
